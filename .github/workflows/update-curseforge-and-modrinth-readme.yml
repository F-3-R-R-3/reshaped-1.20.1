name: Update Modrinth + CurseForge README

on:
  push:
    paths:
      - README.md
    branches:
      - master

jobs:
  update-readme:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - name: Read README.md
        id: readme
        run: |
          README_CONTENT=$(jq -Rs . < README.md)
          echo "README_CONTENT=$README_CONTENT" >> $GITHUB_ENV

      - name: Update Modrinth description
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        run: |
          curl -X PATCH "https://api.modrinth.com/v2/project/reshaped" \
            -H "Authorization: $MODRINTH_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"body\": $README_CONTENT}"

      - name: Update CurseForge description
        env:
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
        run: |
          curl -X PATCH "https://addons-ecs.forgesvc.net/api/v2/addon/395494" \
            -H "x-api-key: $CURSEFORGE_TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"description\": $README_CONTENT}"
